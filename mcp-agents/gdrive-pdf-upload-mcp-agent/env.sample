# Google Drive PDF Upload MCP Agent - Environment Variables
# Copy to .env and fill in values

# OpenAI API key (for LLM-powered tool selection)
OPENAI_API_KEY=sk-...

# Google Drive OAuth - paths (optional, defaults to ./credentials.json and ./oauth_tokens.json)
# GDRIVE_CREDENTIALS_PATH=./credentials.json
# GDRIVE_TOKENS_PATH=./oauth_tokens.json

# Default Google Drive folder ID for uploads (get from folder URL)
# GDRIVE_DEFAULT_FOLDER_ID=your_folder_id_here

# Agent port
# GDRIVE_AGENT_PORT=8003
