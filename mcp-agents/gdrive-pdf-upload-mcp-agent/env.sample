# Google Drive File Upload MCP Agent
# Copy to .env and fill in values

# Default Google Drive folder ID for uploads (get from folder URL)
GDRIVE_DEFAULT_FOLDER_ID=your_folder_id_here

# OAuth paths (optional - defaults to ./credentials.json and ./oauth_tokens.json)
# GDRIVE_CREDENTIALS_PATH=./credentials.json
# GDRIVE_TOKENS_PATH=./oauth_tokens.json

# MCP server port
# GDRIVE_MCP_PORT=8082
